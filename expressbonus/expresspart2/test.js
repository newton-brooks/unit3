// var game = {
//     "computer": ["rock", "paper", "scissors"]
// }

// const pick = (obj) => {
//    return obj.computer[Math.floor(Math.random() * 3)]
// }


// console.log(pick(game))
var user = 'rock'
var pick = 'paper'

console.log()


switch (true) {
    case user === "rock" && pick === "rock":
        return `Tie`
        break;
case user === "rock" && pick === "paper":
         return `Lose`
         break;
case user === "rock" && pick === "paper":
         return `Lose`
         break;
case user === "rock" && pick === "paper":
         return `Lose`
         break;
case user === "rock" && pick === "paper":
         return `Lose`
         break;
case user === "rock" && pick === "paper":
         return `Lose`
         break;
case user === "rock" && pick === "paper":
         return `Lose`
         break;
case user === "rock" && pick === "paper":
         return `Lose`
         break;
case user === "rock" && pick === "paper":
         return `Lose`
         break;

    default:
        break;
}
 
